name: Publish to PyPI

on:
  push:
    tags: [ '*' ]


jobs:
  test:
    name: Dummy Release job
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Simulate release
        run: |
          echo "✅ Release workflow placeholder running on branch: ${{ github.ref }}"
          echo "This is a fake workflow to activate the Run workflow button."
