name: CI

on:
  push:
    branches: ["main"]     # auto on main
  pull_request:
  workflow_dispatch:        # manual run from any branch

jobs:
  test:
    name: tests (py${{ matrix.python }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.12", "3.13"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up micromamba
        uses: mamba-org/setup-micromamba@v2
        with:
          environment-file: conda/env.yaml
          cache-environment: true
          create-args: >-
            python=${{ matrix.python }}
          init-shell: bash

      - name: Install build
        shell: bash -l {0}
        run: |
          python -m pip install -U pip build

      - name: Install package with test dependencies
        shell: bash -l {0}
        run: |
          pip install -e ".[test]"

      - name: Run tests with coverage
        shell: bash -l {0}
        run: |
          pytest --cov-report=xml

      - name: Upload coverage.xml
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.python }}
          path: coverage.xml

      # Build & publish the artifacts only once (python 3.12)
      # OPTIONAL : this will be done automatically by release.yml
      #- name: Build sdist and wheel
      #  if: matrix.python == '3.12'
      #  shell: bash -l {0}
      #  run: |
      #    python -m build
      #    ls -lh dist
      #
      #- name: Upload dist artifacts
      #  if: matrix.python == '3.12'
      #  uses: actions/upload-artifact@v4
      #  with:
      #    name: python-package-distributions
      #    path: dist/*
